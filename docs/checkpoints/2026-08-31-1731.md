# Checkpoint — 2026-08-31 17:31

## Session summary

Continuation of the multi-day Ktav spec 0.7.0 hardening effort. This
segment covered three independent review rounds plus one unrelated
side-quest, using an established pattern: read the actual review
report, verify every finding directly against real files/code
(zero-trust, never take review text or sub-agent claims on faith),
group findings into task clusters, delegate implementation to `/rush`
sessions each running in its own `git worktree` (to allow safe
parallel edits to shared files like `spec.md`), then personally
verify every diff before merging into `main`.

**Round 1** (`docs/reviews/2026-08-31-spec-0.7-full-review.md`, 9
findings): grouped into 4 task clusters (A: representability/numeric
domain; B: root/key dispatch ambiguity; C: grammar/textual
consistency; D: release-scope completion). A/B/C ran via 3 parallel
rush+worktree sessions, all merged cleanly. Group B's agent found and
correctly flagged (rather than silently resolving) a genuine 3-way
conflict: fixture `bracket_in_key` expected `InvalidKey` for
`[bad]: 1`, but the actual Rust reference parser returns
`UnterminatedInlineCompound` — I verified this myself via a `cargo
run --example` probe against `../rust`, then fixed the spec text
(added a `[`/`{`-first-byte precedence rule to § 5.0.1) and renamed
the fixture. Group D (spec#4's `unrepresentable/` category, reason
codes, README runner-contract docs) was done directly by me — deemed
too design-heavy to delegate blind. Explicitly deferred, per the
user's own framing ("library ergonomics are the bindings' problem,
spec correctness is ours"): the cross-repo half of rust#12 (C ABI
extension, 6 bindings, CI) and the strict-mode/lossy-scalar
structured-error contract (a from-scratch normative feature — no
existing spec text at all).

A side-quest arose from an external Habr comment (quoted verbatim)
asking about Ktav's key-quoting/escaping design. While investigating
the reported "misleading warning" in the playground, I found and
fixed a real bug in `ktav-lang/ktav-lang.github.io` (the `site` repo,
separate from `spec`): `assets/bundle.js` — what `index.html` actually
serves, since GitHub Pages has no CI/build step — had not been
rebuilt since commit `4cb8dff` bumped the vendored WASM to ktav 0.6.3,
so the live site was silently serving stale pre-0.6.3 behaviour.
Rebuilt via `php build/build.php --js`, fixed the `auditKtavKeys`
warning heuristic (was flagging harmless cases like interior
whitespace), committed (`8e9a617`), and — with explicit user
authorization — **pushed to `site`'s `origin/main`** (the only push
this whole session; `spec` itself was never pushed). Also filed
`spec#7` (JSON5-style optional key quoting, an RFC for a future 1.0+
major version, explicitly not this cycle) and drafted a Habr reply in
`promo/habr-comment-reply-key-quoting.md` in the `ktav-lang` meta
repo — the user asked to shorten its last two paragraphs, which I did
via `Edit`, but **this edit was never committed** (see Repo state
below — it's the meta repo's only session-relevant uncommitted
change).

**Round 2** (`docs/reviews/2026-08-31-spec-0.7-full-review-round-2.md`,
8 findings, largely bugs I myself introduced across Rounds 1/D):
grouped into 3 clusters (E: representability-model restructure +
`unrepresentable/` fixture ambiguity; F: numeric-domain writer-side
symmetry + Float overflow; G: RU/ZH sync + bracket-precedence wording
+ README staleness). Before delegating, I personally worked out the
exact fix design for the two hardest findings (splitting
`representable` into a document-root check plus an internal
`node-representable` recursive predicate; mirroring § 8.1's
numeric-domain caveat onto § 8.2 for writer-conformance) so the rush
prompts wouldn't have to invent the design blind. All three groups
ran in parallel worktrees and completed successfully. Two merge
conflicts arose from groups touching adjacent text (CHANGELOG.md
`### Fixed` section between E and G; the § 5.9.0 Float bullet between
E and F) — both resolved by hand, verified line-by-line. **Group F's
own final report claimed "2 appendix bullets × 3 languages" were
added to each spec's self-changelog Appendix A, but verification
found EN was actually missing both bullets (RU and ZH had them,
confirmed by re-checking my own initial miscount)** — I wrote and
committed the two missing EN bullets myself before merging, rather
than trusting the agent's summary. I also independently verified
Group F's three new Float boundary fixtures (`1e9999`, `-1e9999`,
`1e-9999`) against the real Rust parser via a fresh `cargo run`
probe — all three matched exactly (String, String, Float 0.0
respectively).

All `spec` work this session lives on `main` at `6f0c6bf`, entirely
local — **nothing in `spec` has been pushed**. All temporary
worktrees (`spec-wt-a` through `spec-wt-g`) and their branches have
been removed after merging; `.rush/stdin/*.prompt`/`.out`/`.err`
scratch files were deleted after each group's verification.

## Active goal

None — no `/goal` Stop hook is armed in this session.

## TaskList

Empty. `TaskList` returned "No tasks found" — tasks #35–52 (covering
Round 1's groups A–D and Round 2's groups E–G, plus the earlier
Habr/playground side-quest) were all created and marked `completed`
in the course of this session; none remain open or pending.

## Decisions

- **Zero-trust verification is non-negotiable, even for my own prior
  work.** Every review finding, every rush agent's diff, and every
  rush agent's own final-report *claim* (not just its diff) gets
  independently re-checked — this session caught two cases where an
  agent's summary didn't match its actual committed diff (the
  `bracket_in_key` 3-way conflict it correctly flagged instead of
  guessing, and Group F's EN-appendix-bullets gap it didn't notice at
  all).
- **Design-heavy or foundational normative text gets worked out by me
  first, then handed to rush as a fully-specified fix** — not left for
  a sub-agent to invent under-specified. Applied to the
  `unrepresentable/` fixture schema (Round 1, Group D) and the
  `representable`/`node-representable` split (Round 2, Group E).
- **Structured-error/strict-mode work (rust#12) stays explicitly
  deferred**, per the user's own stated principle: the spec's job is
  correctness and completeness regardless of implementation cost;
  binding-side ergonomics and the cross-repo ABI/CI rollout are the
  bindings' problem to solve afterward, not something to rush into
  this session's scope.
- **`versions/0.6/` remains untouched for the entire session** — every
  fix, including ones that trace back to pre-existing bugs in the
  already-shipped 0.6.4 spec, was applied only under `versions/0.7/`.
- **The `site` repo push was a one-off, explicitly authorized
  exception** to the standing "never push without an explicit ask"
  rule — the user approved it specifically for that fix; it does not
  extend to `spec` or to any future push in either repo.

## Open questions

- None pending from the user. The last unresolved thread — whether to
  shorten the Habr reply draft — was answered and applied; the only
  loose end is that this shortened version was never committed (see
  Repo state), not a question needing an answer.
- Not yet decided by the user (surfaced but not asked as a blocking
  question): whether/when to start the deferred rust#12 cross-repo
  work (C ABI extension + 6 bindings + CI), and whether/when to begin
  authorizing spec 0.7.0's actual release (`versions.ktav` update,
  git tag, GitHub release) — neither has been raised again since being
  deferred, so both remain open-ended, not scheduled.

## Repo state

`spec` (this repo, `D:\dev\ktav-lang\spec`):

```
?? docs/
```

```
6f0c6bf Merge branch 'review2-fix/group-f'
b5a2334 Add the two missing EN Appendix A 0.7.0 bullets for Sec 8.2/Float overflow
3a8fe61 Merge branch 'review2-fix/group-e'
7a6c67e Fix § 5.9.0 flat-predicate bug: root check + node-representable split
b3608dd Editorial fixes: trimmed-line bracket precedence, RU/ZH 5.9.8 sentence, README layout/ABI
```

Working tree is otherwise clean (`docs/` itself is this untracked
checkpoints directory). Not pushed — `origin/main` is many commits
behind. No worktrees remain (`git worktree list` shows only `spec`
itself).

Other repos touched this session (outside this checkpoint's own
directory, noted here for continuity):

- **`site`** (`D:\dev\ktav-lang\site`) — clean, at `8e9a617`, **pushed**
  to `origin/main` with explicit authorization.
- **`ktav-lang` meta repo** (`D:\dev\ktav-lang`) — at `b3b0ab8`, not
  pushed. One session-relevant uncommitted change:
  `promo/habr-comment-reply-key-quoting.md` was shortened per the
  user's request but never committed. (`promo/habr-article.md` shows
  modified too, but that predates this session and is unrelated.)
