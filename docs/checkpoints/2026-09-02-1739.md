# Checkpoint — 2026-09-02 17:39

## Session summary

Continuation of the multi-day ktav-lang spec 0.7.0 hardening effort, this segment covering independent review rounds 8b through 11 (each produced by an external `fh`-style review agent, written to `D:\dev\ktav-lang\docs\reviews\`, and processed in this `spec` repo at `D:\dev\ktav-lang\spec`). Pattern per round: read the review file, decompose findings into TaskList entries (per standing user instruction), implement fixes via `/wrush` (rush CLI, always inside an in-tree `worktrees/<slug>` git worktree, never the primary checkout directly), zero-trust-verify every diff before merging, clean up worktrees/branches, mark tasks completed, delete tasks when the user asks.

Round 8b (6 findings) and round 9 (6 findings, tasks #62-68) were fully resolved: RU/ZH normative-content restorations across many spec sections, a new `scripts/check_translation_parity.py` structural gate (RFC-keyword + code-block + paragraph/list/table counts per section, comparing EN vs RU/ZH), `scripts/validate_corpus.py` hardening (InvalidUtf8 bidirectional oracle check, `--boundary-manifest-lock`), CHANGELOG fixes, BOM/InvalidUtf8 documentation, a moved `boundary-fixtures.lock.json`, and an Appendix A historical-date fix.

**Critical incident this segment**: during round 10's remediation (tasks #69-74, implemented via 6 parallel `/wrush` sessions that each hit provider rate limits and needed resuming), several resume/merge steps wholesale-copied `spec.ru.md`/`spec.zh.md` from stale worktrees while adding their own fix, silently reverting a *different* already-landed fix in the same file. This happened three times in a row across commits `192ecae`, `6dc3c4b`, and even my own "fix" commit `8e9096d` (which only checked one level back and missed that its restoration source was itself already corrupted). Round 11's review caught this precisely (`docs/reviews/2026-09-02-1641-spec-0.7-full-review-round-11.md`) by diffing the full net range from `bee03ce` instead of trusting the latest commit message. Root cause: applying an entire file from a worktree/historical-commit instead of patching only the specific diff hunk needed. **Fixed this session** (commit `5623465`) by extracting exact historical diffs via `git show <commit> -- <file>` and applying them as targeted find-and-replace edits, then explicitly verifying the full net diff `bee03ce..HEAD` contains every expected phrase from every claimed fix — not just running the parity checker (which stayed green through all three rounds of the regression, since paragraph/list/RFC-keyword counts are insensitive to this class of semantic-content-swap).

Round 10 (tasks #69-75) restored RU/ZH Sec 5.4 (array-item algorithm), Sec 6.2/6.3/6.4/6.8/6.11 (error category conditions), fixed a real ZH Sec 5.6.1 writer-correctness bug (wrong fallback-form direction for `))` collisions — was telling the writer to switch to verbatim in both directions instead of the correct stripped↔verbatim bidirectional rule), extended the parity checker for duplicate-EN-section-numbers and unnumbered sections (Abstract, Appendix A-D), added a CHANGELOG pre-1.0 exception, and fixed the wrong 0.1.1 historical date.

Round 11 (tasks #76-77, just completed) fixed the regression described above and extended the parity checker once more to cover front matter (exactly one h1, Version-field parity, release/draft-status parity via a dated-vs-draft heuristic, and a front-matter line-shape whitelist that catches stray appended paragraphs) — deliberately NOT using generic paragraph/list counts for front matter, since RU/ZH legitimately carry an extra translation-disclaimer blockquote EN lacks.

Standing decisions carried through this whole segment: checkpoint files under `spec/docs/checkpoints/` are dev-log artifacts, not documentation — the user explicitly decided (twice, when rounds 8b/9/10/11 reviews all separately flagged them) they stay tracked as-is, exempt from the "all docs ship EN/RU/ZH" policy; this is NOT an open item, don't re-raise it. `rust`/ecosystem work (rust#12, structured errors) is explicitly out of scope for this cycle — `PLAN_0.7_RELEASE.md` in the meta repo (`D:\dev\ktav-lang`) already has a note recording this (commit `6a1504c` there). Nothing has been pushed all session — local `main` is currently many commits ahead of `origin/main` (`762192d`); GitHub Actions has never run this workflow.

## Active goal

None currently set (an earlier `/goal "реализуй исправления с помощью /wrush"` and a `/babygoal` with the same text were both superseded/completed during round 10; the babysit cron `8a472b6c` was explicitly deleted once round 10's TaskList emptied out, per its own self-delete contract). No Stop-hook condition is currently armed.

## TaskList

Empty — all tasks from rounds 8b through 11 (#53 through #77) were completed and then deleted (per explicit user "удали все таски" after round 9, and implicitly superseded/cleared by task completion in rounds 10-11; current `TaskList` call returns "No tasks found").

## Decisions

- Never wholesale-copy `spec.ru.md`/`spec.zh.md` from a worktree or historical commit when merging a fix — always extract and apply the specific diff hunk (`git show <commit> -- <file>`, then targeted Edit), and always verify the full net diff from the round's baseline commit afterward, not just the latest commit. This is now a hard-learned rule after 3 rounds of the same regression.
- The translation-parity checker (`scripts/check_translation_parity.py`) is a *structural* proxy, not a semantic one — it cannot and will not ever catch a translation being replaced with a differently-worded but structurally-similar (same paragraph/list/keyword counts) version. Don't treat a green parity-checker run as proof that translation content is unchanged; always additionally verify the actual net diff contains the expected content when checking for regressions.
- Checkpoint files stay in the public `spec` repo as-is (dev-log artifacts exemption) — settled, don't re-litigate.
- rust#12 / ecosystem work stays out of scope for the current spec-only cycle — settled, documented in the meta repo's `PLAN_0.7_RELEASE.md`.
- `/wrush`'s mandatory in-tree `worktrees/` convention (vs `/rush`'s external-sibling convention used in earlier rounds this session) is now the standard going forward per explicit user invocation of `/wrush` — `.gitignore` already has the `/worktrees/` entry.

## Open questions

- None outstanding. Round 11's finding 3 (checkpoints) is explicitly decided (not open). The review process may continue with further rounds (12, 13, ...) if the user runs another external review — no indication yet whether round 11 is the last one before a GO decision.

## Repo state

```
On branch main
Your branch is ahead of 'origin/main' by 73 commits.
  (use "git push" to publish your local commits)

Untracked files:
	docs/checkpoints/2026-09-02-1739.md  (this checkpoint file itself, about to be committed)
```
```
b056ac9 Extend parity checker to cover front matter: h1 count, Version, release status (round-11 finding 2)
5623465 Restore ZH Sec 5.6/5.6.1 and RU/ZH Sec 6.2/6.3/6.4/6.8/6.11 (round-11 finding 1)
1e213d1 Extend parity checker: reject duplicate EN section numbers, cover unnumbered sections (round-10 finding 2)
8e9096d Fix: previous commit accidentally reverted the Sec 5.4 fix (my own merge error)
6dc3c4b Fix wrong 0.1.1 historical date in Appendix A: 2026-05-08 -> 2026-05-10 (round-10 finding 5)
192ecae Restore RU/ZH Sec 5.4: full array-item-line algorithm conditions (round-10 finding 1)
9ab18cd Restore RU/ZH Sec 6.2/6.3/6.4/6.8/6.11: error category conditions (round-10 finding 1)
41c2ffe Add pre-1.0 exception to CHANGELOG; restore literal MUST in RU (round-10 finding 3)
```

All automated checks green as of this checkpoint: `python scripts/validate_corpus.py versions/0.7/tests --require-unrepresentable --require-boundary --boundary-manifest-lock scripts/locks/boundary-fixtures.0.7.lock.json` → PASS; `versions/0.6/tests` → PASS; `scripts/test_validate_corpus.py` → 20 tests OK (1 POSIX-only skip on Windows); `scripts/test_check_translation_parity.py` → 16 tests OK; `scripts/check_translation_parity.py` against real EN/RU/ZH → 0/92 numbered-section mismatches, front matter OK; `git diff --check bee03ce..HEAD` → clean. No worktrees left open (`git worktree list` shows only the primary checkout).
