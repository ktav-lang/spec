# Pre-release marker — v0.7.0-pre1 (2026-09-02 20:54)

## What this is

`HEAD` at commit `53ce3e8` is tagged locally as `v0.7.0-pre1` — an
internal "stable pre-release" bookmark for the ongoing spec 0.7.0
draft work, requested by the owner after independent review round 13
(both sub-rounds, 18:11 and 20:32) closed with zero open findings.

**This is not a release.** Nothing about the draft's own metadata
changed:

- `versions/0.7/spec.md` / `.ru.md` / `.zh.md` still declare
  `**Version:** 0.7.0`, `**Date:** (unreleased — 0.7 draft, normative
  text and fixtures only)`.
- `versions.ktav` still reports `stable`/`latest = 0.6.4`.
- No `CHANGELOG.md` version header was added; `Unreleased` stays as is.
- The tag was **not pushed** — `origin/main` is untouched, still at
  `762192d`. Nothing was published to any registry or remote.

## Why this point

By this commit, independent `fh`-agent review rounds 8b through 13
(6 rounds, ~20 findings total across RU/ZH translation-parity
restorations and `scripts/check_translation_parity.py` hardening) are
all fully closed:

- RU/ZH normative-content parity with EN verified section-by-section,
  including full recovery from the 3-round wholesale-copy regression
  incident (see `2026-09-02-1739.md` in this same directory for the
  full incident writeup).
- `scripts/check_translation_parity.py`: 30 unit tests green; catches
  section-inventory drift, RFC 2119 keyword loss (fence-aware),
  paragraph/list/table-count loss, heading-level mismatches,
  front-matter h1/Version/release-date (exactly-one, digit-bounded,
  calendar-validated)/field-line-count mismatches, and unclosed
  trailing fences.
- `scripts/validate_corpus.py`: 20 unit tests green; both
  `versions/0.6/tests` and `versions/0.7/tests` corpora structurally
  valid (`--require-unrepresentable --require-boundary
  --boundary-manifest-lock`).
- README/CHANGELOG kept in sync across EN/RU/ZH.
- Round 13's own verdict (both sub-rounds): **GO** for the spec-only
  draft state, zero High/Medium findings remaining.

## What's still needed before an actual 0.7.0 release

Not implied or authorized by this tag — each of these needs the
owner's separate explicit go-ahead, per this repo's standing rules:

1. `git push` to `origin/main` and confirm a green `corpus-validate`
   run on GitHub Actions (the workflow has never executed remotely —
   `origin/main` doesn't have it yet).
2. Replace draft metadata: `Date` line in all three languages,
   `Appendix A`'s `0.7.0` entry, `CHANGELOG.md`'s `Unreleased` header
   becoming a versioned one.
3. Update `versions.ktav`'s `stable`/`latest` pointer.
4. Push and push the real release tag (`v0.7.0`) — a separate act from
   this internal `v0.7.0-pre1` bookmark.

## Tag details

```
git tag -a v0.7.0-pre1 -m "..." 53ce3e8
```

Local only. To inspect: `git show v0.7.0-pre1`. To remove if it turns
out to be premature: `git tag -d v0.7.0-pre1` (safe — never pushed).
