# Checkpoint — 2026-09-02 09:40

## Session summary

Continuation of the multi-day Ktav spec 0.7.0 hardening effort. This
segment processed review rounds 5 through 8 of `versions/0.7/` (spec
0.7.0 draft), using the established pattern: read the review report,
verify every finding zero-trust against actual files/code (never take
review text at face value), group findings into clusters, delegate
implementation to `/rush` sessions each in its own `git worktree`
(for safe parallel edits to shared files like `spec.md`), then
personally verify every diff before merging into `main`.

**Round 5** (`docs/reviews/2026-08-31-2245-...round-5.md`): found the
round-4 `boundary-fixtures.json` manifest omitted two real
domain-dependent fixtures (`big_overflow_to_string`,
`min_positive_subnormal` — the latter my own miscategorization error)
and exempted whole fixtures instead of individual leaves. Fixed by
redesigning the manifest as leaf-level (JSON Pointer `path` +
`boundary_class` in `{integer_range, float_range, float_underflow,
float_precision}`), rewriting § 8.1/§ 8.2 to match, adding the two
missing fixtures. Delegated the RU/ZH mirror via one `/rush` worktree.

**Round 6**: confirmed round 5's fixes were correct and complete; new
findings were RU/ZH never translating §§ 7.1–7.8/10.1–10.6 (just "see
English") and the total absence of automated corpus CI. Fixed via
three parallel `/rush` worktrees: full RU translation of those
sections, full ZH translation, and a new `scripts/validate_corpus.py`
+ `.github/workflows/corpus-validate.yml` (structural checks: fixture
triple completeness, UTF-8/JSON validity, manifest JSON-Pointer
resolution, closed `boundary_class` set) — self-tested by
intentionally breaking a fixture and confirming the validator catches
it, then restoring.

**Round 7**: I did not process this round when its report first
landed (a real gap in my own tracking — the user later asked "мы это
ревью обработали?" about round 5, and separately round 7 sat
unprocessed until round 8's own review re-surfaced its still-open
findings). Its two substantive findings — §§ 7.3/7.7 examples using
the wrong raw-marker syntax (`key: :: value` instead of
`key:: value`), and the six confirmed false-green mutations in
`validate_corpus.py` (accepts `NaN` JSON, duplicate object keys,
unknown `expected_error` values, empty/absent manifest for 0.7,
`../`-path traversal in a manifest `fixture` field) — are being fixed
now, bundled into round 8's own remediation (see below), specifically
because I finally read round 7 in full while investigating round 8.

**Round 8** — commissioned via a fresh `fh` (Fable 5.1, high-effort)
sub-agent per explicit user request, told to write an independent
Rust-based reference parser+writer from the EN spec text alone and
run it against the whole corpus (not just read prose). This caught a
genuine **Blocker**: § 5.9.5(d)'s raw-marker condition was phrased as
domain-*dependent* ("is not a token the parser would classify... as
something other than a String"), which literally contradicts the five
existing boundary-overflow fixtures I had personally verified against
real Rust in earlier rounds — a minimum-domain parser DOES classify
`9223372036854775808` as a String (rule 15 fallback), so the old
condition said bare form applies, but the fixtures correctly require
`::` (a wider-domain reader would otherwise silently reclassify the
bare form as an Integer). Fixed by making condition (d)
domain-*independent* ("does not match the integer/float grammar of
§ 3.6, regardless of domain fit"). The agent also found: two
`§ 5.9.6` round-trip bugs (bare-form item starting with `::` loses
content on re-parse; the "first item looks like a pair" test didn't
match § 5.0.1 rule 6's actual phase-1 shape-only test — confirmed
empirically that the REAL Rust 0.6.4 reference has exactly this bug
today, `emit_canonical` producing bare `a,b: 1` that re-parses as
`InvalidKey`); undefined dotted-key-prefix "reopen after sibling"
semantics where **the real Rust reference's two parsers (tree vs.
event) actually disagree with each other**; a missing `InvalidUtf8`
error category (§ 3.1/§ 9.3 required rejecting bad UTF-8 but § 6 had
no category for it, making it structurally impossible to write a
fixture); plus escape-provenance ambiguity, unstated
blank-line-in-verbatim behavior, and ~10 more Medium/Low items
(RU/ZH translation gaps, README self-contradictions, stale
post-superseded-manifest wording, an undefined "version-pragma"
term, etc.). The `fh` agent wrote its own report to
`D:\dev\ktav-lang\docs\reviews\2026-09-02-0838-...round-8.md` and
committed it in the **meta repo** (`D:\dev\ktav-lang`, a different
git repository from `spec`) per explicit instruction — verified the
commit afterward and caught/confirmed the agent's own self-correction
of a `\uXXXX`-escape-swallowed-by-tooling corruption in its first
draft (exactly the corruption class this whole session guards
against), confirmed clean in the final committed version.

I personally fixed the four most semantically sensitive round-8
items directly (each its own commit, each with new fixtures verified
against the real Rust reference where the behavior predates 0.7):
the § 5.9.5 Blocker, both § 5.9.6 bugs, the dotted-prefix reopen
semantics (§ 5.3.2/§ 6.3 — resolved in favor of permissive merge,
matching the project's own "flat alternative to nesting... mix
freely" design intent and the tree parser's actual behavior; the
event parser's stricter `SyntheticReopen` behavior is now a
documented, out-of-session-scope Rust bug), and the new `InvalidUtf8`
category (§ 6.15) with a real invalid-UTF-8-bytes fixture and a
narrowly-scoped validator exemption for exactly that one fixture
class. Prompted directly by the user mid-session ("нужно как-то,
чтобы спек тесты покрыли все сложности, что мы сейчас решаем много
раундов"), every one of these fixes came with a regression fixture,
not just a prose change — this is now the explicit standing policy
for all remaining work, not just this round.

The remaining round 7+8 findings are currently being fixed via **four
parallel `/rush` worktree sessions**, launched and NOT YET verified or
merged as of this checkpoint:
- `review78-fix/examples-readme` (worktree `spec-wt-r78-examples`):
  §§ 7.3/7.7 raw-marker example fix, § 10.4 control-byte contradiction,
  stale pre-leaf-level-manifest wording in § 5's Integer bullet and
  Appendix A, a leading-BOM policy + fixture, § 6.5/§ 6.6 wording
  precision, the undefined "version-pragma" removal, § 5.8.5's
  overclaim about `(`/`((` always erroring, and README's
  "no commas/no escape table" self-contradiction + stale layout tree.
- `review78-fix/validator` (worktree `spec-wt-r78-validator`):
  hardening `scripts/validate_corpus.py` against all six confirmed
  false-green mutations (strict JSON via `parse_constant`/
  `object_pairs_hook`, closed `expected_error` enum read live from
  § 6, `--require-unrepresentable`/`--require-boundary` flags wired
  into the 0.7 CI step only, fixture-path traversal hardening) plus a
  new `scripts/test_validate_corpus.py` unit/mutation-test suite.
- `review78-fix/ru-zh-parity` (worktree `spec-wt-r78-parity`): RU § 4
  missing two inline BNF comments; ZH § 4 restoring the full BNF
  grammar (currently just "see English"); RU/ZH § 5.8.5 missing an
  entire paragraph about `(`/`((` at inline-scalar start; ZH § 5.6.1
  losing its normative MUST.
- `review78-fix/provenance-blanklines` (worktree
  `spec-wt-r78-provenance`): § 5.2's escape-provenance rule extended
  to explicitly force String for ANY escaped body regardless of what
  it decodes to (closes an ambiguity for `\u0031`-style escaped
  digits/keywords/parens), plus three new fixtures; § 5.6's verbatim
  bullet clarified so a blank/whitespace-only content line is
  explicitly a content line, not silently dropped per § 5.1 rule 1,
  plus two new fixtures — all five new fixtures' expected values
  already worked out and either verified against real Rust
  (blank-line cases; predates 0.7) or derived by careful spec-text
  reasoning and flagged as such (the `\uXXXX` cases; genuinely new to
  0.7, unverifiable against the not-yet-updated Rust reference).

All `spec` work this session lives on `main` at `0844fc8`, entirely
local — **nothing has been pushed** (the `site` repo push from
earlier in the multi-day session remains the sole, explicitly
authorized exception on any repo, and is unrelated to this segment).
`docs/` (this checkpoints directory) remains the only untracked path
in `spec`.

## Active goal

None — no `/goal` Stop hook is armed.

## TaskList

Empty — `TaskList` returned "No tasks found". No tasks were created
this segment; work was tracked through direct execution and the four
in-flight `/rush` worktree branches named above, not through the
TaskList mechanism.

## Decisions

- **New standing policy, stated explicitly by the user mid-session**:
  every fix to spec prose that describes testable behavior MUST come
  with an accompanying regression fixture, not just a text edit —
  the corpus (plus the validator) is the mechanism that's supposed to
  stop future rounds from re-discovering the same class of bug. Round
  8's four self-fixed items and all four in-flight `/rush` groups
  follow this.
- **Dotted-key reopen-after-sibling semantics resolved in favor of
  permissive merge**, not the stricter "error if a sibling pair
  intervenes" reading — chosen to match the project's own documented
  design intent (README: dotted keys as a "flat alternative to
  nesting... mix freely", useful for "partial edits... without
  restructuring") and confirmed empirically against the real Rust
  tree-parser's actual behavior. The event parser's disagreement
  (`SyntheticReopen`) is now a documented, known, out-of-scope Rust
  bug relative to the corrected spec text — not something this
  session fixes in Rust code.
- **CHANGELOG collapse (three superseded manifest-schema descriptions
  narrated as sequential draft history) remains deferred**, now for
  the third consecutive review round flagging it — still treated as
  a pre-release finalization step, not a per-iteration blocker, since
  the draft is still actively changing and collapsing now would need
  redoing after every subsequent round.
- **Round 7 was genuinely missed** (not deferred on purpose) until
  round 8's own review re-surfaced its findings — flagged here so a
  future session doesn't assume "processed rounds = every round
  whose file exists", and so the two round-7-specific fixes (raw
  marker examples, validator false-greens) are correctly attributed
  to both rounds in commit messages rather than only round 8.
- **`fh` sub-agent given real git-commit authority** for the round-8
  review report specifically (explicit, scoped user instruction,
  distinct from this session's standing default of the orchestrator
  doing all commits) — verified the resulting commit and the agent's
  own mid-task self-correction of a tooling-induced escape-sequence
  corruption before trusting the report's content.

## Open questions

- None currently pending from the user. The four in-flight `/rush`
  groups are running unsupervised between turns; the natural next
  step on resume is to check their completion notifications, then
  zero-trust-verify each diff (encoding, `git diff --check`,
  `python scripts/validate_corpus.py` on both 0.6 and 0.7, spot-check
  fixture semantics) before merging, exactly as every prior round in
  this segment did.
- Not asked about, still open-ended and unscheduled (surfaced
  repeatedly by review rounds but explicitly out of this session's
  scope by earlier user decision): the structured-error/strict-mode
  contract (`rust#12`, `spec#4`) and updating the Rust reference to
  actually implement/run the 0.7 corpus (`rust` repo remains on
  commit `0b5aac2`, `SPEC_VERSION = "0.6"`).

## Repo state

`spec` (`D:\dev\ktav-lang\spec`):

```
?? docs/
```

```
0844fc8 Fix round-8 finding 9: add InvalidUtf8 error category (Sec 6.15)
8ae7a3c Fix round-8 finding 4: define dotted-prefix reopen semantics, align Sec 5.3.2 with Sec 6.3
e9fb288 Fix round-8 Blocker + two writer round-trip bugs in Sec 5.9.5/5.9.6
47e0b48 Merge branch 'review6-fix/zh-translate'
31d141a Translate spec §§ 7.1–7.8 and §§ 10.1–10.6 into ZH
52034f4 Merge branch 'review6-fix/ru-translate'
```

Working tree otherwise clean. Not pushed — `origin/main` is ~30+
commits behind. Four worktrees currently checked out alongside the
main one, each mid-`/rush`-run as of this checkpoint (not yet merged,
not yet even confirmed complete):

```
D:/dev/ktav-lang/spec                   0844fc8 [main]
D:/dev/ktav-lang/spec-wt-r78-examples   0844fc8 [review78-fix/examples-readme]
D:/dev/ktav-lang/spec-wt-r78-parity     0844fc8 [review78-fix/ru-zh-parity]
D:/dev/ktav-lang/spec-wt-r78-provenance 0844fc8 [review78-fix/provenance-blanklines]
D:/dev/ktav-lang/spec-wt-r78-validator  0844fc8 [review78-fix/validator]
```

`ktav-lang` meta repo (`D:\dev\ktav-lang`, noted for continuity —
this is where `docs/reviews/*.md` and `PLAN_0.7_RELEASE.md` actually
live, a separate git repository from `spec`):

```
 M .gitignore
 M promo/habr-article.md
 M promo/habr-comment-reply-key-quoting.md
?? docs/reviews/2026-08-23-0.6.4-release-review.md
?? docs/reviews/2026-08-27-spec-0.7-followup-review.md
?? docs/reviews/2026-08-31-1816-spec-0.7-full-review-round-3.md
?? docs/reviews/2026-08-31-2051-spec-0.7-full-review-round-4.md
?? docs/reviews/2026-08-31-2245-spec-0.7-full-review-round-5.md
?? docs/reviews/2026-08-31-spec-0.7-full-review-round-2.md
?? docs/reviews/2026-08-31-spec-0.7-full-review.md
?? docs/reviews/2026-09-01-0801-spec-0.7-full-review-round-6.md
?? docs/reviews/2026-09-01-1030-spec-0.7-full-review-round-7.md
(... several more untracked promo/*.md files pre-dating this session,
unrelated ...)
```

```
2c991bd Round-8 spec review: restore literal escape spellings swallowed by tooling
1e2202d Add spec 0.7 full review round 8 (corpus executed independently; 5.9.5 vs canonical fixtures blocker)
b3b0ab8 Draft reply: quoting/colon-escaping question + spec#7 RFC
```

The `.gitignore`/`promo/habr-article.md`/
`promo/habr-comment-reply-key-quoting.md` modifications and the long
tail of untracked `promo/*.md`/`docs/reviews/*.md` files pre-date
this session segment (most from before this multi-day project's
review-rounds work even started) — none of it was touched this
segment; noted here only so a future session doesn't mistake it for
new, unexplained drift.
