# Checkpoint — 2026-09-03 13:47

## Session summary

Continuation of the multi-day ktav-lang spec 0.7.0 hardening effort, resumed via `/resume` from checkpoint `2026-09-03-0806.md`. This segment: (1) closed round-14 review's remaining items already covered in the prior checkpoint; (2) processed round-15 review (`docs/reviews/2026-09-03-1048-spec-0.7-full-review-round-15.md`, 4 findings) — F1 (`##`-key canonical-form conflict: three normative spots said bare-with-escape while form-selection rule + fixtures required quoted) and F2 (inline unterminated quoted key allowed two error categories instead of one) fixed together in commit `533f4a0`; F3 (parity checker didn't compare grammar production RHS syntax, only LHS names) fixed in `400b69b` with a `GRAMMAR_TOKEN_RE`/`extract_grammar_productions`/`_is_pure_bnf` mechanism plus a regression test; F4 (public GitHub issue spec#7 still said "1.0+ candidate") closed by actually retitling the issue and posting a decision comment via `gh issue edit`/`gh issue comment` (user explicitly authorized this public action via `AskUserQuestion`), with local doc updates in the meta repo (`132f135`); (3) ran an independent GO/NO-GO rush audit of round-15's fixes in an isolated worktree (session `round15-audit`) — verdict **GO**, all 4 findings confirmed genuinely closed, but it also independently discovered a **new bonus finding**: §3.6 (Number Literals) uses bare LHS names without `<...>` brackets in its grammar fence, so the ENTIRE grammar-parity gate (both round-14's LHS-set check and round-15's new RHS check) silently never fires there — demonstrated via a `"0x"`→`"0o"` mutation that passed undetected; not yet fixed, flagged as follow-up; (4) processed round-16 review (`docs/reviews/2026-09-03-1217-spec-0.7-full-review-round-16.md`, 3 findings) via `/wrush` — F1 (a malformed backslash terminal `"\"` written where the notation's own preamble requires the 4-byte `"\\"`, in 6 locations × 3 languages = 18 total, silently defeating F3's own RHS check for `<key-escape>`/`<escapable-byte>`) and F3-fixture-typo done directly by me (byte-precision risk too high to delegate — same `\uXXXX`-in-tool-call-JSON corruption bug from earlier sessions applies to literal `\`/`"` sequences too, confirmed empirically this segment), F2 (the RHS check's `_is_pure_bnf` silently EXCLUDED any non-tokenizing fragment rather than fail-closed, conflating legitimate semi-formal prose with real corruption) implemented directly by me after building a definitive prose-vs-syntax classification of all 38 §4 productions (added `SEMI_FORMAL_PROSE_LHS` allowlist of exactly 10 genuinely-prose productions, extended `GRAMMAR_TOKEN_RE` for `[char-class]` and the one-off `1*ws` ABNF spelling so `<hex-digit>`/`<sep-end>` become checked instead of assumed-prose, made `extract_grammar_productions` return `(productions, malformed)` with EN-level fatal + per-translation-reported fail-closed behavior, added 4 regression tests including a protective test pinning the real spec.md's exact checked-production count at 28) — all committed as `00bcbf9` + `c4a3729` in a `/wrush` worktree, fast-forward-merged to main (clean, no divergence); (5) mid-conversation the user proposed a large architecture change — replace the 3 hand-authored spec.md/.ru.md/.zh.md files with a source-of-truth of small per-section JS-object units (`{en, ru, zh}` fields) assembled by a generator, specifically to make translation parity structurally guaranteed instead of heuristically checked after the fact; resolved via `AskUserQuestion` rounds: JS modules (not JSON), generated `.md` files STAY committed to git (readable on GitHub, not CI-only), per-SECTION granularity (97 units, not per-paragraph which would be hundreds of tiny files), migrate ALL 97 sections in one pass (not a pilot subset); (6) reframed the migration approach mid-design to eliminate LLM-transcription corruption risk: instead of `/wrush` agents manually re-typing/splitting content (high corruption risk at ~9000+ lines × 3 languages), the plan is a MECHANICAL extraction script (reusing `check_translation_parity.py`'s already-proven `parse_file()` section-boundary logic) that byte-slices the current files into `versions/0.7/content/<unit>/{meta.js,en.md,ru.md,zh.md}` plus a `manifest.js`, paired with a Node.js generator (`scripts/build_spec.mjs`) that reassembles them and a `--check` mode proving byte-identical round-trip — decomposed into TaskList #101 (single `/wrush` agent: build both scripts + run the full migration + verify byte-identical) blocking 4 parallel QA-batch tasks #102-105 (`/wrush` agents doing read-only boundary-leakage spot-checks across the 97 extracted units, split into quarters by manifest order) blocking #106 (final integration: fix any QA-flagged extraction bugs by patching the extraction script and re-running — never hand-patching generated content — then wire CI, decide `check_translation_parity.py`'s residual role, update contributor docs); `/babysit` armed (`f233e6e0`, 15-min interval) per `/babygoal`'s contract; task #101 was `in_progress` (rush session `content-migration-core`, worktree `spec/worktrees/content-migration-core`, `--timeout 60m`, orchestrator-mode with an active worker sub-agent, confirmed alive via `rush sessions locks` at last check) when this checkpoint was written — **this rush run's outcome is NOT yet known**, and none of tasks #102-106 have started.

**Operational discoveries reinforced this segment**: (a) literal `\"`/`\\`/`\uXXXX` sequences typed directly into a Bash/Write tool-call parameter risk being silently decoded to the actual character somewhere in the harness pipeline BEFORE reaching the shell/file — worked around repeatedly via Python scripts using `chr(92)` concatenation instead of literal backslash-escape text in the tool-call itself; this bug is specific to MY OWN tool-call parameter pipeline, not a universal LLM issue, which is WHY finding-1-style byte-precision grammar fixes were done directly rather than delegated to `/wrush` (a separate CLI process without the same pipeline) — though this distinction was reasoned about, not empirically re-confirmed against a `/wrush` agent specifically. (b) worktree-diff conflicts recur whenever a package worktree is diffed against a `main` that has since advanced from OTHER already-merged packages — a Docs-package patch this segment tried to revert package C's already-merged §5.3.3 fix; caught by reading the diff BEFORE applying (not after), reverted, and only the non-conflicting hunks re-applied via a hand-trimmed patch file — reinforces the standing zero-trust "read the diff before you apply it, not just before you commit it" practice.

## Active goal

None. No `/goal` Stop-hook armed. `/babysit` cron `f233e6e0` (15-min interval, session-only) is armed and governs continuation of tasks #101-106 above.

## TaskList

### in_progress
- #101 Design + implement content/ split-and-rebuild tooling, run full mechanical migration — rush session `content-migration-core`, worktree `D:\dev\ktav-lang\spec\worktrees\content-migration-core`, branch `content-migration-core` off `main` at `c4a3729`, `--timeout 60m`, launched via `run_in_background`, last liveness check showed PID alive with an active orchestrator-mode worker sub-agent.

### pending
- #102 QA batch 1 (first quarter of `content/manifest.js` order) — blockedBy #101
- #103 QA batch 2 (second quarter) — blockedBy #101
- #104 QA batch 3 (third quarter) — blockedBy #101
- #105 QA batch 4 (fourth quarter) — blockedBy #101
- #106 Final integration: fix QA findings, wire CI, decide `check_translation_parity.py`'s fate, update contributor docs — blockedBy #102, #103, #104, #105

### recently completed (this segment, not in TaskList — round-15/16 review fixes predate the content-migration TaskList and were tracked only via commits, not tasks)
- Round-15 F1+F2 (`533f4a0`), F3 (`400b69b`), F4 (public issue update + local docs, `132f135` in meta repo)
- Round-16 F1+F3 (`00bcbf9`), F2 (`c4a3729`)
- Independent round-15 audit (rush session `round15-audit`, worktree removed after, verdict GO, one bonus finding: §3.6 grammar-gate blind spot, not yet actioned)

## Decisions

- Content-migration source format: JS modules (`export default {...}`), not JSON — user's explicit choice, "как вы написали" (matching my proposed example).
- Generated `spec.md`/`.ru.md`/`.zh.md` stay committed to git as readable-on-GitHub artifacts; NOT a CI-only build output.
- Per-SECTION granularity (97 units), not per-paragraph — user reversed their own initial "по параграфам" phrasing after I flagged the ~10x file-count/effort difference and asked to confirm.
- Migrate all 97 sections in one pass, not a small pilot first — user's explicit choice when asked.
- Migration MUST be mechanical (a parsing/slicing script), never LLM re-transcription of content through an agent's own reasoning — my own reframing mid-design, not directly asked by the user but justified by the corruption-risk analysis above; the user has not objected (no turn to react yet, since task #101 is still running).
- `scripts/check_translation_parity.py` is NOT being retired preemptively — task #106 decides its residual role once the exact `build_spec.mjs --check` gate exists; default lean is "keep both, belt-and-suspenders" unless #106 finds a clear reason to drop it.
- The §3.6 bare-LHS grammar-gate blind spot (round-15-audit bonus finding) is explicitly deferred, not folded into the current content-migration work — different concern (parity-checker coverage gap, not content-authoring architecture).

## Open questions

- None blocking right now. The content-migration work is fully specified in TaskList #101-106's descriptions and running unattended under `/babysit`.
- Natural next decision points once #101 reports back: (a) does the extraction achieve genuine byte-identical round-trip on the first attempt, or does the agent need a resumed/re-delegated pass to fix boundary bugs; (b) once #106 completes, whether to also fix the independently-discovered §3.6 grammar-gate blind spot in the same wave or as a separate follow-up.

## Repo state

**spec** (`D:\dev\ktav-lang\spec`):
```
On branch main
nothing to commit, working tree clean
```
```
c4a3729 Make grammar-RHS parity check fail-closed on malformed syntax (F2, round-16)
00bcbf9 Fix malformed backslash terminal in Sec 4 grammar (F1); fix typo in fixture note (F3) (round-16)
400b69b Harden translation parity checker against grammar production RHS corruption (F3, round-15)
533f4a0 Fix ## canonical-form conflict and inline UnterminatedQuotedKey ambiguity (F1+F2, round-15)
600dda3 Translate quoted-keys sections into RU/ZH (F4, round-14)
```
`git worktree list` shows one active worktree beyond the primary checkout: `spec/worktrees/content-migration-core` (branch `content-migration-core`, task #101's rush agent working there). All structural checks were last green at `c4a3729` (before #101 started): `validate_corpus.py` (0.6 and 0.7), both unit-test suites (37 + 20 tests), `check_translation_parity.py` against real files (`OVERALL: PASS`, 0 mismatching sections), `git diff --check`.

**meta repo** (`D:\dev\ktav-lang`):
```
?? "2026-06-01 15_24_03.png"  (pre-existing, deliberately left untracked — clipboard-paste artifact)
?? "2026-06-01 16_15_53.png"  (same)
?? "2026-06-01 16_36_19.png"  (same)
?? examples/                  (pre-existing, deliberately left untracked — contains node_modules/vendor/build output, needs its own .gitignore before it could be committed)
```
```
2aeed44 Add spec 0.7 review round 16
132f135 Record spec#7 public issue update (F4, round-15)
f67767f promo: add .promo assets, awesome-list PR drafts, and post drafts
65cff2a promo: add cross-platform post/reply drafts, trim habr article and quoting reply
a7034b6 docs: add spec 0.7 review round reports (rounds 2-13) and 0.6.4 release review
```
Nothing pushed to `origin/main` in either repo this whole session.
