# Checkpoint — 2026-09-04 07:59

## Session summary

Continuation of the multi-day ktav-lang spec 0.7.0 hardening effort. This segment processed six consecutive full-spec review rounds (17 through 22) via delegated workers, each working in an isolated git worktree under `spec/worktrees/<slug>`, with every result independently re-verified (zero-trust: re-run `node scripts/build_spec.mjs --check`, `node --test scripts/test_build_spec.mjs`, both `validate_corpus.py` invocations, `check_translation_parity.py`, and its unit tests) before integration, then removing the worktree/branch. Review reports themselves live in the **parent repository** under `<meta-repo>/docs/reviews/`, not in `spec/`.

**Round 17** (findings 1-3 fixed; finding 4/commit-rename explicitly declined by the user via `AskUserQuestion` and has stayed declined every subsequent round it's been re-flagged): closed-world `build_spec.mjs` validator hardened (exact manifest/directory-set match, frontmatter invariants, exact per-unit file sets, terminal-newline invariant), extractor archived to `scripts/archive/` with `--force`'s destructive wipe removed; embedded-token extraction added for 10 semi-formal-prose grammar productions instead of blanket-excluding them; RU/ZH `content/README.md` translations published, root READMEs synced.

**Round 18** (findings 1-5 fixed): unit-directory entries now require `ent.isFile()` (symlink bypass closed), `title` requires exactly `{en,ru,zh}`; non-last-unit terminal-LF check tightened to reject 3+ trailing LFs; mixed-prose grammar gate extended with `LANGUAGE_INDEPENDENT_ATOM_RE` for hex/control-byte atoms (`0x20`, `DEL`, `LF`, `CR`, `VT`, `FF`); async test-fixture cleanup race fixed; content READMEs' false "zero .md files" claim corrected, all three READMEs made mandatory (agent's own judgment call, flagged and accepted).

**Round 19** (findings 1-4 fixed, 2 parallel worktrees since file scope was genuinely disjoint): an independent second opinion was used for finding 1's severity (executable-JS-bypass in `body-N.js`) before delegating — verdict: real risk was overstated as a security issue (no real trust boundary exists), but a **narrow shape-scanner** for `body-N.js`'s exact literal-template shape is independently valuable for catching escaping mistakes; implemented exactly that. Separately: `COMPOUND_ATOM_PAIR_RE` added for LF/CR/VT/FF/DEL label-value pairing; `extract_embedded_tokens`'s "ordered" docstring promise made genuinely true.

**Round 20** (findings 1-3 fixed, 2 parallel worktrees): `manifest.js`/`meta.js` switched from `dynamic import()` to `readJsonDefault` (raw text, strip `'export default '` prefix, `JSON.parse` — cannot execute code), with the `manifest.js` top-level-entry-type-check-before-read ordering bug fixed at the same time; `body-N.js`'s already-redundant `dynamic import()` + defense-in-depth cross-check removed entirely (the shape-scanner's own decoded value is now used directly, closing the last content-tree execution surface); body escape grammar tightened to require `\${` specifically (was accepting bare `\$`); grammar-parity gate extended with absolute (not just cross-language) constants for `control-threshold=0x20` and `tab=0x09`, since these are fixed universal facts, not translatable — this ALSO catches a corruption applied identically to all three languages, which no cross-language comparison could ever catch. I personally fixed one small side-effect doc-staleness bug (three content READMEs still described `meta.js` as dynamically imported) directly rather than delegating.

**Round 21** (findings 1-3 fixed, single worktree; finding 4/CRLF was explicitly folded into finding 2's fix rather than implemented separately, per the review's own suggested unification): `meta.js` doc examples across all three languages fixed to remove a trailing `;` that the real JSON parser now rejects (a genuine authoring-contract regression introduced by round 20's dynamic-import-to-JSON-parse switch); `readJsonDefault` now requires the ENTIRE file to be byte-identical to `'export default ' + JSON.stringify(value, null, 2) + '\n'` — this single canonical-byte check closes BOTH duplicate-JSON-key detection (JSON.parse silently collapses `{"en":"old","en":"new"}`) AND CRLF/trailing-whitespace rejection in one mechanism; strict UTF-8 decoding (`TextDecoder('utf-8',{fatal:true})`) replaces lenient `fs.readFileSync(path,'utf8')` everywhere; lone-UTF-16-surrogate detection added for `title` and body text (code-point iteration, not a hand-rolled regex, to avoid subtle bugs). Agent empirically verified byte-identical round-trip against all 103 real `meta.js` + `manifest.js` BEFORE committing to the design.

**Round 22** (findings 1-3 fixed, single worktree; finding 4/commit-rename declined again): UTF-8 BOM (`EF BB BF`) was silently stripped by `TextDecoder`'s default `ignoreBOM:false` BEFORE the canonical-byte comparison ran, so a BOM-prefixed file would slip through undetected — fixed by rejecting the BOM at the raw-byte level inside `decodeUtf8Strict` itself (single choke point covers manifest/meta/body). Two test-quality findings also fixed: a duplicate-top-level-key regression test had TWO independent reasons to fail (inline non-canonical nested formatting PLUS the duplicate itself), rebuilt to isolate exactly one cause; the README `meta.js`-example acceptance test only validated the EN copy, now parametrized across all three languages.

**Meta-repository discovery at checkpoint time (not yet actioned)**: the next review report already exists under `<meta-repo>/docs/reviews/` and is committed in the parent repository. **This round has not been read or processed yet**.

## Active goal

None. No continuation hook is armed.

## TaskList

Empty. All work this segment was tracked via delegated work plus immediate zero-trust verification and integration, not via a persistent task list.

## Decisions

- Finding severity is not always taken at face value: round 19's finding 1 (executable-JS bypass) was independently re-evaluated before deciding how to fix it. The recommendation (narrow shape-scanner, not full AST, not JSON conversion) was followed exactly.
- The two `fix(spec):`-prefixed commit subjects (`52174f6`, `aef69e7`) remain intentionally unrenamed — the user explicitly chose "Оставить как есть" the first time this was raised (round 17) and it has been silently skipped every round since without re-asking, since re-litigating an already-made decision on every recurrence would be noise. This will need to be revisited only if the user changes their mind or before an eventual push.
- Canonical-byte-comparison (round 21) was deliberately chosen over a custom duplicate-aware JSON parser because it's a single mechanism that closes multiple findings at once (duplicate keys, CRLF, trailing whitespace, indentation drift) with less code and less risk of a hand-rolled parser bug.
- Content-tree files (`manifest.js`, `meta.js`, `body-N.js`) are now **fully non-executable** as of round 20 — nothing under `versions/0.7/content/` is ever `dynamic import()`-ed; everything is read as raw bytes, strictly UTF-8-decoded, and either `JSON.parse`d or scanned by a narrow hand-verified grammar.

## Open questions

- **Round 23 review is unprocessed** (`docs/reviews/2026-09-04-0753-spec-0.7-full-review-round-23.md` in the meta-repo) — natural next step when the user asks to continue.
- The `fix(spec):` commit-rename question (finding 4/5/6 depending on the round) will keep resurfacing in every future review round until either the user authorizes the history rewrite or the two commits get folded into history some other way (e.g. squash-on-eventual-release). No action needed now; just don't re-ask every round — it's a settled "no" until told otherwise.
- No other open questions; each round's fixes were fully verified and merged before moving to the next.

## Repo state

**spec** (this repository):
```
On branch main
nothing to commit, working tree clean
```
```
947dae9 build: reject UTF-8 BOM before decode and harden regression oracles
1fb7d71 build: enforce canonical JSON bytes, strict UTF-8, and reject lone surrogates
213f411 docs: make content READMEs' meta.js examples strict canonical JSON
dbdbcb6 docs: correct content READMEs' stale dynamic-import/package.json claims
4be8b29 Assert '< 0x20' threshold and tab 0x09 pairing as fixed absolute constants
```
No worktrees beyond the primary checkout. All structural checks green at HEAD: `node scripts/build_spec.mjs --check` (silent, exit 0), `node --test scripts/test_build_spec.mjs` (54/54), both `validate_corpus.py` invocations (0.6 and 0.7, PASS), `check_translation_parity.py` (PASS, 0 mismatches), `test_validate_corpus.py` (20 tests, 1 pre-existing platform skip).

**meta repository** (outside this repository):
```
?? "2026-06-01 15_24_03.png"  (pre-existing, deliberately left untracked)
?? "2026-06-01 16_15_53.png"  (same)
?? "2026-06-01 16_36_19.png"  (same)
?? examples/                  (pre-existing, deliberately left untracked — node_modules/vendor)
```
```
69d4104 Add spec 0.7 review round 23
f6dcd9b Add spec 0.7 review round 22
3b83e9c Add spec 0.7 review round 21
```
Nothing pushed to `origin/main` in either repo this whole session.
