
四个序列 `{}`、`[]`、`()`、`(())` 可以作为 value-start 出现(依据
§ 5.2 规则 5 及相应的 inline 规则),并表示:

| 序列 | Value |
|----------|-------|
| `{}`     | 空 Object |
| `[]`     | 空 Array |
| `()`     | 空 String |
| `(())`   | 空 String |

这些形式的存在是为了让空复合值可以用单独一行表达。

