export default {
  en: `
Previously: *Inline non-empty compound*. In 0.5.0+, inline non-empty
compounds are valid (§ 5.8). This number is reserved to avoid
renumbering older error catalogs. Implementations MUST NOT emit an
error labelled \`InlineNonEmptyCompound\` when parsing 0.7.0 documents.

`,
  ru: `
Ранее: *Inline non-empty compound*. В 0.5.0 inline non-empty
compounds валидны (§ 5.8). Реализации MUST NOT выводить ошибку с
меткой \`InlineNonEmptyCompound\` при парсинге 0.7.0-документов.

`,
  zh: `
原:Inline non-empty compound。0.5.0 内 inline non-empty 复合值
有效(§ 5.8)。实现 MUST NOT 在解析 0.7.0 文档时输出标签为
\`InlineNonEmptyCompound\` 的错误。

`,
};
