export default {
  en: `
Input MUST be valid UTF-8. Implementations MUST reject malformed
UTF-8 sequences with an explicit \`InvalidUtf8\` error (§ 6.15) rather
than silently substituting replacement characters.

`,
  ru: `
Ввод MUST быть валидным UTF-8. Реализации MUST отвергать
некорректные UTF-8 последовательности явной ошибкой \`InvalidUtf8\`
(§ 6.15), а не молча подставлять символы-заменители.

`,
  zh: `
输入 MUST 是有效 UTF-8。实现 MUST 以明确的 \`InvalidUtf8\` 错误
(§ 6.15)拒绝非法的 UTF-8 序列,而不是静默地替换为替换字符。

`,
};
