export default {
  en: `
A \`{\` or \`[\` appearing in a value-position not followed by a matching
\`}\` / \`]\` on the same line is an \`UnterminatedInlineCompound\` error
(§ 5.8).

`,
  ru: `
\`{\` или \`[\`, появляющиеся в позиции значения, за которыми на той же строке не следует соответствующий \`}\` / \`]\`, — это ошибка \`UnterminatedInlineCompound\` (§ 5.8).

`,
  zh: `
值位置上出现的 \`{\` 或 \`[\`,若同一行内其后没有匹配的 \`}\` / \`]\` 跟随,是 \`UnterminatedInlineCompound\` 错误(§ 5.8)。

`,
};
