
- **非空 Object 值:** `key: {` 后接换行、每对在缩进 + 1 的行上、
  关闭 `}` 在当前缩进的自身行上。
- **非空 Array 值:** `key: [` 后接换行、每项在缩进 + 1 的行上、
  关闭 `]` 在当前缩进的自身行上。
- **空 Object 值:** `key: {}` 单行。
- **空 Array 值:** `key: []` 单行。
- 除空 `{}` / `[]` 外,inline 复合值**绝不**以规范形式输出。

