
```
## Sample configuration

## --- Network ---
host: localhost
port: 8080

## Authentication settings
auth: {
    enabled: true
    realm: production
}
```

每个以 `##` 为前缀的行都是注释,会被忽略。不在 `##` 对开头位置的
`#` 字节只是一个普通字符。

