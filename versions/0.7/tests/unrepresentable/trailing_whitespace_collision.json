{"value": "))\nx ", "unrepresentable_reason": "TrailingWhitespaceCollision", "note": "Body has two lines: the first trims to exactly '))' (forcing the stripped-form fallback), the second ('x ') has trailing whitespace. The stripped form strips trailing whitespace from every content line on emission, so the fallback would silently lose it. See Sec 5.9.7."}
