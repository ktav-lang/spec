{"expected_error": "InvalidUtf8", "note": "The .ktav file's raw bytes end with 0xFF, which is never a valid UTF-8 start or continuation byte (Sec 3.1, Sec 6.15). This is the one invalid/ fixture whose .ktav input is deliberately not valid UTF-8 -- a conformance runner MUST NOT reject it as a malformed fixture file; it IS the fixture."}