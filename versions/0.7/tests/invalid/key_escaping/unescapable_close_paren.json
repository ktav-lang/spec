{"expected_error": "BadEscapeSequence", "note": "`)` has no named § 3.7 escape sequence, same as `(`. As of 0.7.0 a literal `)` in a key must be spelled `\\u0029` (no named form exists); a bare `\\)` remains BadEscapeSequence."}
