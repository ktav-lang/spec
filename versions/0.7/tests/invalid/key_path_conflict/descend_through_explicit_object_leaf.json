{"expected_error": "KeyPathConflict", "note": "`a.x` is a leaf Integer inside the explicit inline Object `a`; the dotted path `a.x.y` tries to descend through it as an Object (§ 5.3.2 BlockedByValue)."}
