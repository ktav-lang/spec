{"expected_error": "DuplicateKey", "note": "Keys differ only by a whitespace code point (U+00A0 NBSP) at the trailing edge of the key segment, immediately before the separator; trimming happens before the duplicate-name check, so both segments decode to the same key \"isAdmin\"."}
