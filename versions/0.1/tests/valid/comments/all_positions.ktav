# top-level comment before everything
port: 8080
# between pairs
name: demo

# before array
items: [
    a
    # inside array, not an item
    b
# another comment, still inside array
    c
]
# trailing
