>>>>> lang=en

Two pairs in the same Object with the same effective key are a
`DuplicateKey` error (§ 5.5).

>>>>> lang=ru

Две пары в одном Object с одинаковым эффективным ключом — это ошибка
`DuplicateKey` (§ 5.5).

>>>>> lang=zh

同一 Object 中具有相同有效键的两个对是 `DuplicateKey` 错误(§ 5.5)。

