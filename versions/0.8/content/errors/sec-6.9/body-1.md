>>>>> lang=en

Previously: *Invalid typed scalar*. In 0.5.0+, typed markers `:i` /
`:f` no longer exist; this number is reserved. Implementations
MUST NOT emit an error labelled `InvalidTypedScalar` when parsing
0.8.0 documents.

>>>>> lang=ru

Ранее: *Invalid typed scalar*. Начиная с 0.5.0 типизированные
маркеры `:i` / `:f` больше не существуют; этот номер
зарезервирован. Реализации MUST NOT выводить ошибку с меткой
`InvalidTypedScalar` при парсинге 0.8.0-документов.

>>>>> lang=zh

原:Invalid typed scalar。自 0.5.0 起,类型标记 `:i` / `:f`
不再存在;此编号已保留。实现 MUST NOT 在解析 0.8.0 文档时输出
标签为 `InvalidTypedScalar` 的错误。

