>>>>> lang=en

Previously: *Inline non-empty compound*. In 0.5.0+, inline non-empty
compounds are valid (§ 5.8). This number is reserved to avoid
renumbering older error catalogs. Implementations MUST NOT emit an
error labelled `InlineNonEmptyCompound` when parsing 0.8.0 documents.

>>>>> lang=ru

Ранее: *Inline non-empty compound*. Начиная с 0.5.0 inline non-empty
compounds валидны (§ 5.8). Этот номер зарезервирован, чтобы избежать
перенумерации старых каталогов ошибок. Реализации MUST NOT выводить
ошибку с меткой `InlineNonEmptyCompound` при парсинге 0.8.0-документов.

>>>>> lang=zh

原:Inline non-empty compound。自 0.5.0 起 inline non-empty 复合值
有效(§ 5.8)。此编号已保留,以免对较旧的错误目录重新编号。实现
MUST NOT 在解析 0.8.0 文档时输出标签为 `InlineNonEmptyCompound`
的错误。

